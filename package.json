{
  "name": "@dirty-matchbox/di-node-service",
  "version": "1.0.0",
  "main": "./dist/index.js",
  "scripts": {
    "build": "tsc",
    "release": "standard-version && git push --follow-tags && npm publish"
},
  "dependencies": {
    "@dirty-matchbox/logger": "*",
    "@dirty-matchbox/database": "*",
    "awilix": "^8.0.1",
    "awilix-express": "^8.0.0"
  }
}
